name: Auto Grade

on:
  push:
    branches: [ main ]

jobs:
  grade:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt || true

      - name: Run tests
        run: |
          echo "Running tests..."
          python tests/test_assignment.py

      - name: Save results
        run: |
          python tests/test_assignment.py > result.txt

      - name: Upload results
        uses: actions/upload-artifact@v3
        with:
          name: grading-results
          path: result.txt
